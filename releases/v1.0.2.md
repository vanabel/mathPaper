# Release v1.0.2

## Changes

- Add continuous compilation support:
  - `make watch` (pdfLaTeX) and `make watch-xe` (XeLaTeX) via `latexmk -pvc`.
- README: document watch mode for both Makefile and direct `latexmk` usage.
